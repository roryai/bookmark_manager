<h1>Links :)</h1>

<ul id="links" >
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tags: <%= p link.tags.name_of_tag %>
    </li>
  <% end %>

</ul>
